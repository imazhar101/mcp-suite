{
  "name": "@imazhar101/mcp-elasticsearch-server",
  "version": "1.0.1",
  "description": "Elasticsearch MCP server for search, analytics, and document management",
  "main": "dist/servers/elasticsearch/src/index.js",
  "type": "module",
  "bin": {
    "mcp-elasticsearch": "dist/servers/elasticsearch/src/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "elasticsearch",
    "search",
    "analytics"
  ],
  "author": "Azharuddin <azhar@oneorigin.us>",
  "license": "MIT",
  "dependencies": {
    "@elastic/elasticsearch": "^8.13.4",
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
